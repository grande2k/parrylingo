name: Deploy Nuxt 3 WebApp

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    # Если ваш Nuxt-проект лежит в подпапке — поправьте путь.
    defaults:
      run:
        working-directory: ./parrylingo

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js & pnpm
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'pnpm'

      - name: Enable Corepack (pnpm)
        run: corepack enable

      - name: Install dependencies
        run: pnpm install

      - name: Build Nuxt & generate
        run: |
          pnpm run build
          pnpm run generate

      - name: Archive build artifacts
        run: |
          # выбираем каталог сборки
          if [ -d "dist" ]; then
            SRC="dist"
          elif [ -d ".output/public" ]; then
            SRC=".output/public"
          else
            echo "❌ Не найдена папка сборки (dist или .output/public)"
            ls -R .
            exit 1
          fi
          echo "📦 Архивируем папку $SRC"
          tar -czf site.tar.gz -C "$SRC" .

      - name: Deploy to server
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          SSH_USER:        ${{ secrets.SSH_USER }}
          SSH_HOST:        ${{ secrets.SSH_HOST }}
        run: |
          # кладём ключ и настраиваем SSH
          echo "$SSH_PRIVATE_KEY" > private_key.pem
          chmod 600 private_key.pem
          trap "rm -f private_key.pem" EXIT
          mkdir -p ~/.ssh
          ssh-keyscan -H $SSH_HOST >> ~/.ssh/known_hosts

          # копируем архив на сервер
          scp -i private_key.pem site.tar.gz $SSH_USER@$SSH_HOST:/srv/parrylingo

          # на сервере распаковываем в /srv/parrylingo и убираем старую сборку
          ssh -i private_key.pem $SSH_USER@$SSH_HOST bash -lc "
            set -e
            cd /srv/parrylingo
            # резерв старой версии
            rm -rf site_old
            mv dist site_old 2>/dev/null || mv .output/public site_old 2>/dev/null || true
            # распаковываем новый сайт
            tar -xzf site.tar.gz
            rm -f site.tar.gz
          "
